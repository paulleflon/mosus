/**
 * An available language in the bot as a ISO 639-1 string.
 */
export type AvailableLanguage = 'fr' | 'en' | 'ko';
/**
 * List of available languages in the bot as ISO 639-1 strings.
 */
export const AvailableLanguages: AvailableLanguage[] = ['fr', 'en', 'ko'];